# CLAUDE.md — ease-design

Guidance for Claude Code (and any agent CLI) working **in this repository** to develop
ease-design itself.

> This is a *runtime-neutral config stub*. The real per-runtime adapters that teach a
> host CLI how to *use* ease-design as a design tool are generated by `ui init`
> (Phase 6) — they are not this file.

## What this project is

ease-design is a multi-runtime design-cli. See `README.md` for the concept and
`plans/ease-design/implementation-plan.md` (in the EaseUI repo) for the 8-phase plan.

## Two sources of truth

- **`knowledge/`** — plain-Markdown knowledge core. The host model reads these files
  directly; never duplicate their content into code.
- **`ui` binary** (`src/cli.ts` → `dist/cli.js`) — deterministic, no LLM calls. All
  non-LLM work (autofix, layout validation, token compilation, color math, registry,
  export) is a `ui <subcommand>`, callable via Bash and identical on every runtime.

## Working rules

- Build / check before committing: `npm run typecheck`, `npm run lint`, `npm run build`,
  `npm test` — all must pass (CI runs the same).
- Keep the binary deterministic: pure transforms, no network, no model calls.
- Read selectively — open the `knowledge/` file relevant to the task, not all of them.
- Decisions in `plans/ease-design/brainstorm.md` §6 are locked; do not relitigate them.
