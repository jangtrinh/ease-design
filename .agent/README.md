# .agent/ — Antigravity runtime placeholder

Placeholder for the Antigravity CLI runtime configuration.

The real `.agent/skills/` and `.agent/workflows/` adapters — which teach the Antigravity
CLI how to *use* ease-design as a design tool — are generated by:

```sh
ui init --runtime antigravity
```

See Phase 6 of the implementation plan (kept in the internal design-engine repo).
