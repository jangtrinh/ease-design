Usage: design-os [OPTIONS] COMMAND [ARGS]...

  design-os — the DESIGN-OS conductor over the deterministic `ui` kernel.

Options:
  --install-completion  Install completion for the current shell.
  --show-completion     Show completion for the current shell, to copy it or
                        customize the installation.
  --help                Show this message and exit.

Commands:
  audit      Audit HTML file(s) or a project dir through the deterministic...
  doctor     Verify the design-os runtime: the `ui` kernel, node, and...
  harvest    Extract structured insight/gap candidates from end-of-phase...
  heartbeat  Run each due config task, compare it to its last reading, and...
  update     Refresh the dev-linked toolchain so the global bins serve the...
  vr-matrix  Render each component (page-shot) and gate it against a...
  ui         Raw passthrough to the deterministic `ui` kernel (bytes + exit...
  plugins    List discovered third-party plugins and whether each one mounts.
  reference  Manage the project's reference cache (references/).
  librarian  The studio librarian's deterministic hand — collect gaps for...
